obsolete - 9/22/21

Patch, by running patch-source.sh.


At the prompt, build myCircos, with 

"./install.pl build"


====== each launch ======
Once built, run "launch-inside-docker.sh" at the prompt. This installs the web files in the temporary
filesystem and launches the web server. Do this every time you run.
